3.1 Euler Alias Solver Completeness
Logic: math_utils.h:L80 (quat_to_euler_near)
Note: The current solver is a "Quad-Branch" selection. While it fixes the most common flipping bugs, it only checks one alias per branch.
Recommendation: Consider checking all 4 significant aliases for XYZ sequences if higher precision is needed for extreme rotations.
